# PromptProbe
This project helps us proactively detect and measure prompt-injection weaknesses in LLM apps before attackers exploit them, using a Python-based testing framework with JSON-defined suites, regex scoring, HTML/JSON/CSV reporting, and adapters for OpenAI, Gemini, Claude and HTTP APIs.
